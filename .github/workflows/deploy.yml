# Build the Vite site and deploy it to GitHub Pages on every push to main.
# The site is served from https://<owner>.github.io/<repo>/, so the build
# gets --base=/<repo>/ and the app resolves routes/assets via BASE_URL.
name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run build -- --base=/${{ github.event.repository.name }}/
      # temporary staging host: keep search engines away so it never
      # competes with hark.digital once v2 ships there
      - run: sed -i 's|</title>|</title><meta name="robots" content="noindex, nofollow" />|' dist/index.html
      # SPA fallback: GitHub Pages serves 404.html for unknown paths
      - run: cp dist/index.html dist/404.html
      - uses: actions/configure-pages@v5
        with:
          enablement: true
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
