# Contact form (SendGrid) — set these in your hosting provider's env settings.
# Never commit real values; never expose SENDGRID_API_KEY to the browser.
SENDGRID_API_KEY=SG.xxxxxxxxxxxxxxxxxxxxxx
CONTACT_TO_EMAIL=mike@hark.digital
CONTACT_FROM_EMAIL=noreply@hark.digital
